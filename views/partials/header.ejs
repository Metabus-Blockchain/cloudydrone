<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Bootstrap4 CDN -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/stylesheets/index.css">
    <title>CloudyDrone</title>
</head>
<body>
    <nav id="mainNavbar" class="navbar navbar-expand-lg navbar-light navbar-expand-md bg-white py-0 fixed-top shadow-sm p-2 bg-white rounded">
        <a href="/" class="navbar-brand"><i class="fas fa-cubes"></i> CloudyDrone</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse"  data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <ul class="navbar-nav navbar-nav ml-auto">
                <% if (!currentUser) { %> 
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup">Sign Up</a>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/users/<%= currentUser._id %>">Hello <%= currentUser.firstName %></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">Logout</a>
                        </li>
                        <li id="postButton" class="nav-item mt-auto">
                            <a href="/poster/new" class="btn btn-outline-success btn-sm my-2 my-sm-0">Get Started</a>
                        </li>
                    <% } %>
            </ul>
        </div>
    </nav>


    <div id="alertBox" class="container mt-md-4 mt-sm-6">
        <% if (error && error.length > 0) { %> 
            <div class="alert alert-danger" role="alert">
                <h5><%= error %></h5>
            </div>
        <% } %>
        <% if (success && success.length > 0) { %> 
            <div class="alert alert-success" role="alert">
                <h5><%= success %></h5>
            </div>
        <% } %>
    </div>